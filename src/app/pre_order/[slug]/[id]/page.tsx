import Image from "next/image"

export default function Page({ params }: { params: { id: string } }) {
  return (
    <section className="text-white-100 w-full md:px-12 px-6 py-2 my-6">Checkout Page</section>
  )
}
